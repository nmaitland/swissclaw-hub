# Swissclaw Hub Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=3001

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# Database (for local development)
# For Render, this is automatically set from the database connection
DATABASE_URL=postgresql://user:password@localhost:5432/swissclaw_hub

# Production database (Render sets this automatically)
# DATABASE_URL=postgresql://...

# Authentication
# Username for login (default: admin)
AUTH_USERNAME=admin
# Password for login (must be set)
AUTH_PASSWORD=your_secure_password_here

# Service-to-service authentication
# Token used by MCP server and other services to access protected endpoints
SWISSCLAW_TOKEN=your_secure_service_token_here

# Optional: Bearer token for MCP server to access user-facing endpoints
# Only needed if the Hub requires authentication for all endpoints
# SWISSCLAW_AUTH_TOKEN=

# Session secret (for enhanced auth - if using server/routes/auth.ts)
# Generate with: openssl rand -base64 32
# SESSION_SECRET=your_session_secret_here
