services:
  - type: web
    name: swissclaw-hub
    runtime: node
    plan: pro
    buildCommand: npm install --include=dev && cd client && npm install --include=dev && npm run build && cd .. && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: swissclaw-hub-db
          property: connectionString
      - key: CLIENT_URL
        sync: false
      - key: AUTH_USERNAME
        sync: false
      - key: AUTH_PASSWORD
        sync: false

databases:
  - name: swissclaw-hub-db
    plan: pro
    databaseName: swissclaw_hub
    user: swissclaw
